{
	"name": "behsseui",
	"version": "0.2.0",
	"description": "Add behsseui components to your projects",
	"packageManager": "pnpm@9.13.0",
	"scripts": {
		"build": "turbo run build",
		"cli:build": "cd packages/cli && pnpm build",
		"cli:publish": "cd packages/cli && pnpm build && npm publish",
		"cli:publish:patch": "cd packages/cli && npm version patch && pnpm build && npm publish",
		"cli:publish:minor": "cd packages/cli && npm version minor && pnpm build && npm publish",
		"cli:publish:major": "cd packages/cli && npm version major && pnpm build && npm publish"
	},
	"devDependencies": {
		"turbo": "^2.6.3",
		"vite": "^7.1.12",
		"vite-tsconfig-paths": "^5.1.4",
		"vitest": "^4.0.2"
	},
	"dependencies": {
		"typescript": "^5.9.3"
	}
}
